const express = require('express');
const router = express.Router();
const Community_Challenge = require('../models/community_challenge');

router.post('', async (req, res) => {});

module.exports = router;
